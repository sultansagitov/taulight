package net.result.sandnode.util.ctx;

import net.result.sandnode.messages.IMessage;

public class PublicKeyContext implements ISequentialContext {
    @Override
    public IMessage onMessage(IMessage message) {
        return null;
    }
}
